*.iml
.gradle
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

build/
release/

# Source tests are versioned; only their build output is ignored.
**/build/test-results/
**/build/reports/

*.apk

# Full screens and the areas cropped from the fixture. Both are captures of a
# running app rather than sources, and are re-cropped whenever it changes.
/design-mockup/
/demo-autoclicks/
